
source convertA2RPR.mel;

global proc convertSceneFile(string $scene, string $output){

    file -f -open -iv $scene;
    convertScene();

    select -all;

    file -op "v=0;" -type "mayaBinary" -pr -es $output; 

    quit -abort;
}