global proc string arnoldGetCurrentSceneName()
{
	string $currentSceneName = basenameEx(`file -q -shortName -sceneName`);
	if($currentSceneName == "")
		$currentSceneName = "untitled";
		
	return $currentSceneName;
}

print(arnoldGetCurrentSceneName());